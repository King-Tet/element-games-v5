import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'image.tmdb.org', 'placehold.co'], // Add other domains here
  }};

export default nextConfig;
